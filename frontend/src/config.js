/*const config = {
    API_BASE_URL: process.env.NODE_ENV === 'production'
        ? 'https://toilaconmeo-be.onrender.com/api' // Production API URL
        : 'http://localhost:3000/api' // Local development API URL
};

export default config;*/
const config = {
    API_BASE_URL: 'https://toilaconmeo-be.onrender.com/api'
};

export default config;
